version: 0.0
os: linux
files:
    - source: /
      destination: /home/ubuntu/auth
permissions:
    - object: /home/ubuntu
      owner: ubuntu
      group: ubuntu
      type:
          - directory
          - file
hooks:
    ApplicationStart:
        - location: scripts/ApplicationStart.sh
          timeout: 300
          runas: root
