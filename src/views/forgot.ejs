<div class="card shadow-sm mb-5">
    <div class="card-body pt-sm-5 px-sm-5 pb-sm-3">
        <% if (alert && alert.message) { %>
            <div class="alert alert-<%= alert.variant %>">
                <%= alert.message %>
            </div>
            <% } %>
                <form autocomplete="off" action="/oidc/<%= uid %>/forgot" method="post">
                    <h2>Forgot your password?</h2>
                    <p>

                        No worries, we got your back! Please submit the e-mail
                        address of your account and we will send you a password reset link.
                    </p>
                    <% if (alert && alert.variant !="success" ) { %>
                        <div class="form-group">
                            <label>Email:</label>
                            <input class="form-control" autocomplete="off" required type="email" name="email" />
                        </div>
                        <input type="hidden" name="returnUrl" value="<%= params.return_url %>" />
                        <button type="submit" class="btn rounded-pill btn-primary btn-block">
                            Send password reset link
                            <i class="fas fa-chevron-right" aria-hidden="true"></i>
                        </button>
                        <a class="btn btn-block btn-link" href="/oidc/<%= uid %>/abort">
                            Back to sign in</a>
                        <% } %>
                </form>
    </div>
</div>